# Himanshu Srivastava — Portfolio

A Flask-based portfolio web application.

## Setup & Run

```bash
# Install dependencies
pip install -r requirements.txt

# Run the app
python app.py
```

Then open http://localhost:5000 in your browser.
